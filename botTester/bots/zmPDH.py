name="DESTROYEER_OF_THE_BOTS"
import random
#history=[]
def step(history):
    random.seed(version=2)#zmPDH
    #rnd=int(random.uniform(0, 10))
    ch1=random.choice(['камень', 'ножницы', 'бумага'])
    return ch1
#a=step(history)
#print(a)
